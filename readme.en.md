# dist-zipper
### Usage:
```bash
dist-zipper <input>
```
### Options: 
```bash 
-o, --output <dir>    # Output directory (default: current directory)
-n, --name <name>     # Output file name (default: dist.zip)
-l, --level <1-9>     # Compression level (default: 9)
-h, --help
```
