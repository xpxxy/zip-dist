# zip-dist
### Usage:
```bash
zip-dist <input>
```
### Options: 
```bash 
-o, --output <dir>    #输出目录（默认：当前目录）  
-n, --name <name>     #输出文件名（默认：dist.zip）  
-l, --level <1-9>     #压缩等级（默认：9）  
-h, --help  
```
